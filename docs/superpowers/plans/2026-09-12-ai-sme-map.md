# AI/ML SME Atlas Implementation Plan

> Execute inline in this session. Data-first: compile catalog, then UI, then always-on server.

**Goal:** Local atlas at `http://127.0.0.1:7432` with ≥500 ranked resources.

**Tech:** Python 3 stdlib server, static HTML/CSS/JS, JSON data.

## Tasks

1. Compile `data/resources.json` + `data/graph.json` from harvest + curated winners.
2. Validate unique URL count ≥ 500.
3. Build dual-rail map UI with search, rails, progress.
4. Serve + persist progress; LaunchAgent; open browser.
