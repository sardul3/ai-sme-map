name: atlas-validate
on:
  push:
  pull_request:
  workflow_dispatch:
  schedule:
    - cron: "17 7 * * *"
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python3 -m unittest discover -s tests -v
      - run: python3 scripts/compile_catalog.py
      - run: python3 scripts/validate.py
      - run: python3 scripts/sync_check.py
  links-do:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python3 scripts/compile_catalog.py
      - run: python3 scripts/check_links.py --do-only
